#include <iostream>;

using namespace std;


bool prime(int n, int a[n])
{
    for (int i=0; i<n; i++)
    {
        if (a[i]==1)
        {
            return false;
        }
        for (int j=0; j<=a[i]/2; j++)
        {
            if (a[i]%j==0)
            {
                return false;
            }
            return true;
        }
    }
}


void delete_annoying_primes (int n, int a[n])
{
    for (int i=0; i<n; i++)
    {
        if (prime(n, a[n])==true)
        {
            for (int j=i+1; j<n; j++)
            {
                if (a[j]==a[i])
                {
                    a[j]='delete';
                }
            }
            if (a[i]!='delete') 
            {
            cout<<a[i];
            }
        }
    }
}
    

        
int main ()
{
    int n;
    cin>>n;
    int* a=new int[n];
    for (int i=0; i<n; i++)
    {
        cin>>a[i];
    }
    delete_annoying_primes;
    delete[] a;
    return 0;
}
